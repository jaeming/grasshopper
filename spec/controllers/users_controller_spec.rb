require 'rails_helper'

RSpec.describe UsersController, :type => :controller do

  it "generates a token for users signing up" do
  end

  it "It checks for a token or assigns one if necessary" do
  end
end